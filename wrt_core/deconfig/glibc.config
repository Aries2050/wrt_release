# 系统 LIBC 切换为 glibc（替代 musl）
# 启用后固件所有组件使用 glibc 编译，可原生运行标准 glibc 链接的二进制程序。
CONFIG_LIBC="glibc"
CONFIG_USE_GLIBC=y
# CONFIG_USE_MUSL is not set
CONFIG_PACKAGE_glibc=y

# glibc 兼容层 - 标准 glibc 二进制所需的运行时库
CONFIG_PACKAGE_libpthread=y
CONFIG_PACKAGE_librt=y
